from modules.student import Student
from modules.data import *
from modules.subjects import *

# colocar as disciplinas criadas em subjects.py dentro do array (último atributo)
Student('Vicente Raimundo Anthony da Rosa', '79517258', 1, [p1.getInfo(), cse.getInfo(), logica.getInfo(), md.getInfo(), cdi.getInfo()])